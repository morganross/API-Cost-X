# APICostX API Service

Local FastAPI service for the APICostX self-hosted application.

The API service is designed for one local user, plain SQLite storage, no account system, and secrets loaded only from the project root `.env` file.
